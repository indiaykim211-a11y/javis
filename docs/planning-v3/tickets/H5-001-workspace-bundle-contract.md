# H5-001 Workspace Bundle Contract

## 목표

snapshot, control deck, activity feed를 한 번에 가져오는 workspace bundle contract를 정의합니다.

## 완료 조건

- `/api/workspace` GET
- generatedAt
- snapshot + controlDeck + activityFeed payload
