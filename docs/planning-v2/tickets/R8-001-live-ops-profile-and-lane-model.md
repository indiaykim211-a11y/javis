# R8-001 Live Ops Profile and Lane Model

목표:

- live ops profile / report cadence / re-entry mode / unattended 한도를 모델로 만든다.
- current lane / touchpoint / recovery level을 계산한다.

완료 기준:

- 세션 모델에 저장된다.
- engine에서 lane을 계산할 수 있다.
