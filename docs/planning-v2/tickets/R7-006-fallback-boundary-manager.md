# R7-006 fallback boundary manager

## 목표

- desktop fallback을 기본 전략이 아니라 예외 전략으로 관리합니다.

## 범위

- fallback 허용 조건
- fallback 차단 조건
- fallback 종료 조건
- fallback 이유 로그 구조

## 완료 조건

- native 우선 / fallback 예외 원칙이 분명함
- fallback 진입과 종료를 설명할 수 있음
